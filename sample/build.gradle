
plugins {
    id 'java'
    id 'idea'
    id 'org.springframework.boot' version '1.5.4.RELEASE'
}

repositories {
    jcenter()
}

dependencies {
    compile project(':camflake')
    compile('org.springframework.boot:spring-boot-starter-jersey')
}

uploadArchives.enabled = false
